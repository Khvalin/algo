/**
 * @param {string} s
 * @param {number} k
 * @return {number}
 */
const characterReplacement = function(s, k) {
  const m = new Map()

  for(let i = 0; i < s.length; i++){
    const ch = s[i]
  }
};
